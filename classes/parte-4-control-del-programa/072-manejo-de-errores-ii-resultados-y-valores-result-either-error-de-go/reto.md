# Reto de transferencia — Manejo de errores II: resultados y valores (Result/Either/error de Go)

Haz que `div` devuelva también el resto (`ok=<coc> resto=<r>`) y resuélvelo en **Go** con `(int, int, error)`.

## Criterio de aceptación

- ✅ La salida coincide exactamente con el formato del contrato.
- ✅ Se usa la forma idiomática del lenguaje elegido.
- ✅ Explicas en un comentario qué diferencia (sintáctica/semántica) observaste.
